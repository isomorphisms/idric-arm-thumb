module FGrepLambda

contains : Char -> String -> Bool
contains needle haystack = elem needle (unpack haystack)

%export "arm-thumb:main"
main : IO ()
main = do
  line <- getLine
  if contains 'λ' line
     then putStrLn line
     else pure ()
